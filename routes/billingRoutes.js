"use strict";
// const passport = require('passport');

module.exports = (app) => {
    app.post('/api/stripe', (req, res) => {
       // console.warn('req', req)
       // console.warn('--------------------')
       console.log('res', res)
    });
};